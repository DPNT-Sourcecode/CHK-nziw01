module.exports = function (count) {
    let newCount = {
        Z: count.Z,
        Y: count.Y,
        S: count.S,
        T: count.T,
        X: count.X
    }
    let totalItems = count.Z+count.Y+count.S+count.T+count.X
    let totalDeals = totatotalItems % 3
   for (const item in newCount) {
       count = 0
       while ()
   }

}